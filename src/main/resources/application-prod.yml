spring:
  jpa:
    hibernate:
#      ddl-auto: validate;
      ddlAuto: none
  sql:
    init:
      mode: never
    generate-ddl:
#      mode: always
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/library
    url: jdbc:mysql://localhost:3360/library
    username: root
#    password: pass123
    password: admin
  liquibase:
    contexts: prod
